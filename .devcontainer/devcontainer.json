{
    "name": "neutron",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    "postCreateCommand": "echo xyz",
    "customizations": {
        "vscode": {
            "extensions": [
                "golang.go",
                "ms-vscode.makefile-tools",
                "aleksandra.go-group-imports"
            ]
        }
    },
    "mounts": [
        "source=${localEnv:HOME}${localEnv:USERPROFILE}/.ssh,target=/home/kog/.ssh,type=bind,consistency=cached",
        "source=${localWorkspaceFolder}/ui/.cache/android,target=/home/kog/.android,type=bind,consistency=cached",
        "source=${localWorkspaceFolder}/ui/.cache/gradle,target=/home/kog/.gradle,type=bind,consistency=cached"
    ],
    "privileged": true,
    "capAdd": ["SYS_ADMIN"],
    "runArgs": [ "--security-opt", "apparmor:unconfined", "--network=host" ]
}
